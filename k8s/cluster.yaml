apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: urlshrink-new-cluster
  region: ap-south-1

managedNodeGroups:
  - name: standard-nodes
    instanceType: t3.small
    desiredCapacity: 3
    iam:
      withAddonPolicies:
        imageBuilder: true 
        autoScaler: true